-- account = current_account()
select  current_account()

-- If you have an old account which I don't belive you do.
    -- Example: account = <current_account>.<current_region>
select current_account(), current_region()
